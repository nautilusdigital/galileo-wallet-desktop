{{#if settings.label}}
  <big>{{settings.label}}</big>
  <br>
  {{account-address value=(to-galileo-prefix account.id) truncate=true}}
{{else}}
  {{account-address value=(to-galileo-prefix account.id) truncate=truncate}}
{{/if}}
